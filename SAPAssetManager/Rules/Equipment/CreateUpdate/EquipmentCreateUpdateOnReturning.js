

import libCom from '../../Common/Library/CommonLibrary';

export default function EquipmentCreateUpdateOnReturning(context) {
    libCom.redrawCurrentPageSection(context, 'LocationButtonsSection');
}
