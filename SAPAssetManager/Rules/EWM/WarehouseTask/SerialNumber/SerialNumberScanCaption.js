import { GetSerialNumberScanCaption } from './SerialNumberLib';

/**
 * get the caption for the serial number scan
 * @param {IClentAPI} context 
 * @returns 
 */
export default function SerialNumberScanCaption(context) {
    return GetSerialNumberScanCaption(context);
}
