import FilterOnLoaded from '../../../Filter/FilterOnLoaded';
/**
* @param {IClientAPI} clientAPI
*/
export default function FLWorkOrdersSearchFilterOnLoaded(context) {
    FilterOnLoaded(context); //Run the default filter on loaded
}
